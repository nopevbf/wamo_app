part of 'pages.dart';

class MovieDetailPage extends StatelessWidget{
  final Movies movies;

  const MovieDetailPage(this.movies, {super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}